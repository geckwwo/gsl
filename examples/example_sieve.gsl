fun eratosthenes(n) {
    multiples = [];
    foreach(i in range(2, n+1)) {
        if(!contains(multiples, 1)) {
            print(i);
            foreach(j in range(i*i, n+1, i)) {
                multiples.append(j);
            }
        }
    }
    return multiples;
}

print(eratosthenes(100));